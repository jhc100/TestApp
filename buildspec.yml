version: 0.2
phases:
  pre_build:
    commands:
      - echo Restore started on `date`
      - dotnet restore TestApp.csproj
  build:
    commands:
      - echo Build started on `date`
      - dotnet publish -c release -o ./build_output TestApp.csproj
artifacts:
  files:
    - CodeBuild-TestApp/build_output/**/*
    - scripts/**/*
    - appspec.yml
